export const ROUTES = {
  authors: '/authors',
  books: '/books',
  chapters: '/chapters',
  home: '/',
  myLibrary: '/my-library',
  search: '/search'
}

// regex
export const PASSWORD_REGEX =
  /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,15}$/
